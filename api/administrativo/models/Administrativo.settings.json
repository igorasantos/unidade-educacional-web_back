{
  "connection": "default",
  "collectionName": "administrativo",
  "info": {
    "name": "administrativo",
    "description": "Pessoal administrativo do Departamento e da Pós-Graduação."
  },
  "options": {
    "timestamps": [
      "createdAt",
      "updatedAt"
    ]
  },
  "attributes": {
    "nome_completo": {
      "required": true,
      "type": "string"
    },
    "unidade_atuacao": {
      "required": true,
      "enum": [
        "depto",
        "curso_grad",
        "ppg"
      ],
      "type": "enumeration"
    },
    "vinculo": {
      "required": true,
      "enum": [
        "estatutario",
        "bolsista",
        "celetista_fund_apoio"
      ],
      "type": "enumeration"
    },
    "cargo": {
      "required": true,
      "enum": [
        "assistente_em_administracao",
        "auxiliar_em_administracao",
        "apoio_tecnico_administrativo"
      ],
      "type": "enumeration"
    },
    "sala": {
      "required": true,
      "type": "string"
    }
  }
}